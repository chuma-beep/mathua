// MA parity: the goals Diagnostic doesn't have to be completed at once — the
// session id persists in sessionStorage so Back/refresh resumes it.
export const GOALS_DIAG_KEY = 'mathua_diag_session_goals'
